/**
 * ===================================================================
 * 模块名:        llm/adapter.ts
 * 输入输出:      无独立逻辑，重定向导出 LLMAdapter 类型
 * 数据流向:      types.ts → adapter.ts → deepseek.ts / agent/loop.ts
 * 修改风险点:    未来支持多供应商时需改为动态导出或工厂函数
 * 最近修改:      2026-05-15 — 初始导出
 * ===================================================================
 */

// 破题：本模块不做实现逻辑，只做 LLM 适配器的统一出口。
// 承题：当前仅支持 DeepSeek，因此只做简单 re-export；
//       后续扩展 OpenAI / Anthropic 时，可在此添加工厂函数统一分发。
// [起讲] 统一出口层隔离了调用方（agent/loop.ts）与具体实现（deepseek.ts），
//       降低新增供应商时的侵入性。
// 入手：一行 re-export，零运行时开销，TypeScript 编译期内联消除。

// ==== 起股 ====
// 取：从 types.ts 导入 LLMAdapter 接口。
// 验：编译期检查确保 types.ts 已导出该符号。
// ==== 中股 ====
// 算：无运行时计算，纯类型重定向。
// 转：将内部 types 模块的导出暴露为 llm 包的公共 API。
// ==== 后股 ====
// ✓ 正路径：导入方正确引用 → 编译通过 → 类型安全
// ✗ 降级路径：符号未导出 → 编译报错 → 开发阶段拦截
// ==== 束股 ====
// 给出：导出 LLMAdapter 类型，供 agent/loop.ts 声明适配器变量类型。
// 留下：未来在此添加 createAdapter(provider, config) 工厂，支持多供应商切换。

export { LLMAdapter } from './types';
